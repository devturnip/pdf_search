version: "3.9"

services:
  pdf-search:
    build: .
    container_name: pdf-search
    ports:
      - "8000:8000"
    volumes:
      - ./pdfs:/app/pdfs
      - ./data:/app/data
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
